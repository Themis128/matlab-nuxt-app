{
  "name": "matlab-nuxt-app devcontainer",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",
  "workspaceFolder": "/workspace",
  "features": {
    "ghcr.io/devcontainers/features/python": {
      "version": "3.14"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.vscode-typescript-next",
        "vue.volar",
        "ms-python.python",
        "ms-python.vscode-pylance",
        "ms-playwright.playwright",
        "esbenp.prettier-vscode"
      ],
      "settings": {
        "typescript.tsdk": "node_modules/typescript/lib",
        "typescript.enablePromptUseWorkspaceTsdk": true,
        "javascript.suggest.enabled": true,
        "typescript.suggest.enabled": true
      }
    }
  },
  "postCreateCommand": "bash -lc 'npm ci && cd python_api && python -m pip install --upgrade pip && pip install -r requirements.txt && sudo apt-get update && sudo apt-get install -y awscli tmux || true && npm install -g @sentry/cli || true && chmod +x .devcontainer/health-check.sh .devcontainer/start-dev.sh .devcontainer/start-servers-tmux.sh scripts/shell/start.sh'",
  "containerEnv": {
    "DEVCONTAINER_AUTO_START_SERVERS": "true"
  },
  "postStartCommand": "bash -lc './.devcontainer/health-check.sh; if [ \"$DEVCONTAINER_AUTO_START_SERVERS\" = \"true\" ]; then ./.devcontainer/start-servers-tmux.sh || true; fi; echo Devcontainer ready. Ports forwarded: 3000 (frontend), 8000 (Python API)'",
  "forwardPorts": [
    3000,
    8000
  ],
  "remoteUser": "vscode"
}
