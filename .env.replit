# Replit deployment environment
# Disable Python API features when hosting on Replit (single port)
NUXT_PUBLIC_PY_API_DISABLED=1

# Optional: point to an external Python API if available
# PYTHON_API_URL=https://your-external-api.example.com
