# Python Requirements for MATLAB Project
# Install with: pip install -r requirements.txt
# Or use the setup script: setup_python_env.bat (Windows) or setup_python_env.sh (Linux/Mac)

# Core scientific computing libraries
# Note: numpy>=2.0.0 required for models saved with newer numpy versions
# If you get "No module named 'numpy._core'" error, reinstall: pip install --upgrade --force-reinstall numpy
numpy>=2.0.0,<3.0.0
scipy>=1.10.0

# Data processing
pandas>=2.0.0

# MATLAB file reading (alternative to scipy)
# mat-file-reader>=0.1.0  # Optional, for Node.js alternative

# Optional: For advanced .mat file handling
h5py>=3.8.0  # For HDF5-based .mat files (MATLAB v7.3+)

# Development tools
ipython>=8.0.0  # Enhanced Python shell

# Visualization (optional)
matplotlib>=3.7.0  # For plotting and visualization

# Machine Learning (if needed for analysis)
# Note: Models may have been saved with newer versions. If you get version warnings,
# upgrade: pip install --upgrade scikit-learn
scikit-learn>=1.3.0,<2.0.0  # Optional, for ML analysis
joblib>=1.3.0  # For model serialization
lightgbm>=4.0.0  # Gradient boosting framework
shap>=0.44.0  # Explainability (SHAP values for tree/linear models)
xgboost>=3.1.2  # Extreme Gradient Boosting (ensemble diversity)

# FastAPI and server dependencies
# Updated to FastAPI 0.115+ which supports anyio>=4.0.0 and starlette>=0.28.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# HTTP client for API testing
requests>=2.31.0

# Error tracking and monitoring
sentry-sdk>=1.38.0
